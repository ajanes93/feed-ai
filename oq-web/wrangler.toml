name = "one-question"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"

# Service binding to the OQ API worker (proxied via Pages Function)
[[services]]
binding = "API"
service = "one-question-api"
