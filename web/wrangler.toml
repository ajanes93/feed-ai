name = "feed-ai"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"

# Service binding to the main API worker (proxied via Pages Function)
[[services]]
binding = "API"
service = "feed-ai-api"
