name = "feed-ai-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[triggers]
crons = ["0 17 * * *"]  # 5pm UTC daily (adjust for your timezone)

[[d1_databases]]
binding = "DB"
database_name = "feed-ai"
database_id = "<your-database-id>"

[vars]
ANTHROPIC_API_KEY = ""  # Set via wrangler secret
