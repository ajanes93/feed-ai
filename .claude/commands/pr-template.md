# Update PR Template

Update the PR body template used by the `/pr` command.

## Instructions

1. Read the current template from `.claude/pr-template.md`
2. Show the user the current template
3. Ask the user what they'd like to change (or accept $ARGUMENTS as the new template)
4. Update `.claude/pr-template.md` with the new template
5. Confirm the change
